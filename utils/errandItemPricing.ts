// Placeholder for shared errand item pricing utilities
// Phase 3–4 will implement database + fallback logic
// Printing must NEVER use this utility

// Future exports will include:
// - getItemPrice(itemName: string, categoryId?: string): Promise<number>
// - parseItemPrice(itemName: string): number (synchronous fallback)
